{application,
 influxdb_cli,
 [{description, ""},
  {vsn, "1"},
  {modules, [influxdb_cli_sup, influxdb_cli_worker, influxdb_cli_app, influxdb_cli_client]},
  {registered, []},
  {applications, [kernel, stdlib, inets]},
  {mod, {influxdb_cli_app, []}},
  {env, []}]}.
